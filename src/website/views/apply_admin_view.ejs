<!DOCTYPE html>
<html>
<head>
    <title>View Application - <%= application.username %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="icon" href="/favicon.ico">
    <link rel="stylesheet" href="/custom.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        .main-card {
            background: var(--ranger-card) !important;
            border: 2px solid var(--ranger-gold) !important;
            border-radius: 15px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
        }
        
        .card-header {
            background: var(--ranger-gold) !important;
            color: var(--ranger-bg) !important;
            border-radius: 13px 13px 0 0 !important;
            border: none;
            padding: 1.5rem;
        }
        
        .application-detail {
            background: #3a3a3a;
            border: 1px solid var(--ranger-gold);
            border-radius: 12px;
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            transition: all 0.3s ease;
        }
        
        .application-detail:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
            background: #404040;
        }
        
        .section-header {
            color: var(--ranger-gold);
            font-weight: 600;
            margin-bottom: 1rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .section-header i {
            color: var(--ranger-gold);
            font-size: 1.2em;
        }
        
        .status-badge {
            font-size: 0.9rem;
            padding: 0.6rem 1.2rem;
            border-radius: 25px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .info-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0.75rem 0;
            border-bottom: 1px solid #555;
        }
        
        .info-row:last-child {
            border-bottom: none;
        }
        
        .info-label {
            font-weight: 600;
            color: var(--ranger-gold);
            min-width: 120px;
        }
        
        .info-value {
            color: var(--ranger-white);
            font-weight: 500;
        }
        
        .info-value code {
            background: #555;
            padding: 0.2rem 0.5rem;
            border-radius: 5px;
            font-size: 0.9em;
            color: var(--ranger-gold);
        }
        
        .question-card {
            background: #3a3a3a;
            border: 2px solid var(--ranger-gold);
            border-radius: 12px;
            padding: 1.5rem;
            margin-bottom: 1rem;
            transition: all 0.3s ease;
        }
        
        .question-card:hover {
            border-color: var(--ranger-gold);
            box-shadow: 0 4px 15px rgba(225, 184, 76, 0.2);
            background: #404040;
        }
        
        .question-label {
            font-weight: 600;
            color: var(--ranger-gold);
            margin-bottom: 0.75rem;
            font-size: 1.1em;
        }
        
        .question-answer {
            color: var(--ranger-white);
            line-height: 1.6;
            padding: 1rem;
            background: #2d2d2d;
            border-radius: 8px;
            border-left: 4px solid var(--ranger-gold);
        }
        
        .no-response {
            color: var(--ranger-gray);
            font-style: italic;
            background: #2d2d2d;
            padding: 0.5rem 1rem;
            border-radius: 6px;
            border: 1px dashed var(--ranger-gold);
        }
        
        .btn-custom {
            border-radius: 25px;
            padding: 0.75rem 1.5rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            transition: all 0.3s ease;
        }
        
        .btn-custom:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
        }
        
        .timeline-item {
            background: #3a3a3a;
            border-radius: 10px;
            padding: 1rem;
            margin-bottom: 0.5rem;
            border-left: 4px solid var(--ranger-gold);
        }
        
        .notes-section {
            background: #3a3a3a;
            border: 1px solid var(--ranger-gold);
            border-radius: 12px;
            padding: 1.5rem;
        }
        
        .notes-content {
            background: #2d2d2d;
            padding: 1rem;
            border-radius: 8px;
            border: 1px solid var(--ranger-gold);
            white-space: pre-wrap;
            line-height: 1.6;
            color: var(--ranger-white);
        }
    </style>
</head>
<body>
    <%- include("partials/navbar", { user, active }) %>
    <div id="main-content">
        <div class="container mt-4">
            <% if (query && query.alert) { %>
                <div class="alert alert-success alert-dismissible fade show mt-3" role="alert">
                    <%= query.alert %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            <% } %>
            <% if (query && query.error) { %>
                <div class="alert alert-danger alert-dismissible fade show mt-3" role="alert">
                    <%= query.error %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            <% } %>

            <div class="row justify-content-center">
                <div class="col-12 col-lg-10">
                    <div class="card main-card">
                        <div class="card-header d-flex justify-content-between align-items-center">
                            <h3 class="mb-0">
                                <i class="bi bi-file-earmark-text me-2"></i>Application Details
                            </h3>
                            <div class="d-flex gap-2">
                                <a href="/apply/admin/edit/<%= application.id %>" class="btn btn-warning btn-custom btn-sm">
                                    <i class="bi bi-pencil-square me-1"></i>Edit
                                </a>
                                <a href="/apply" class="btn btn-outline-secondary btn-custom btn-sm">
                                    <i class="bi bi-arrow-left me-1"></i>Back
                                </a>
                            </div>
                        </div>
                        <div class="card-body p-4">
                            <!-- Applicant Information -->
                            <div class="row mb-4">
                                <div class="col-md-6">
                                    <h5 class="section-header">
                                        <i class="bi bi-person-circle"></i>Applicant Information
                                    </h5>
                                    <div class="application-detail">
                                        <div class="info-row">
                                            <span class="info-label">Username:</span>
                                            <span class="info-value"><%= application.username %></span>
                                        </div>
                                        <div class="info-row">
                                            <span class="info-label">User ID:</span>
                                            <span class="info-value"><code><%= application.user_id %></code></span>
                                        </div>
                                        <div class="info-row">
                                            <span class="info-label">Status:</span>
                                            <span class="info-value">
                                                <span class="badge <%= application.status === 'approved' ? 'bg-success' : application.status === 'denied' ? 'bg-danger' : 'bg-warning text-dark' %> status-badge">
                                                    <%= application.status.charAt(0).toUpperCase() + application.status.slice(1) %>
                                                </span>
                                            </span>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <h5 class="section-header">
                                        <i class="bi bi-clock"></i>Timeline
                                    </h5>
                                    <div class="application-detail">
                                        <div class="timeline-item">
                                            <div class="info-row">
                                                <span class="info-label">Submitted:</span>
                                                <span class="info-value"><%= new Date(application.submitted_at).toLocaleString() %></span>
                                            </div>
                                        </div>
                                        <% if (application.updated_at) { %>
                                            <div class="timeline-item">
                                                <div class="info-row">
                                                    <span class="info-label">Updated:</span>
                                                    <span class="info-value"><%= new Date(application.updated_at).toLocaleString() %></span>
                                                </div>
                                            </div>
                                        <% } %>
                                        <% if (application.updated_by) { %>
                                            <div class="timeline-item">
                                                <div class="info-row">
                                                    <span class="info-label">Updated By:</span>
                                                    <span class="info-value"><%= application.updated_by %></span>
                                                </div>
                                            </div>
                                        <% } %>
                                    </div>
                                </div>
                            </div>

                            <!-- Application Responses -->
                            <h5 class="section-header">
                                <i class="bi bi-chat-text"></i>Application Responses
                            </h5>
                            
                            <% questions.forEach(question => { %>
                                <div class="question-card">
                                    <div class="question-label">
                                        <%= question.label %>
                                    </div>
                                    <div class="question-answer">
                                        <% 
                                            // Map form field IDs to database column names
                                            let fieldValue = null;
                                            if (question.id === 'foundUnit') {
                                                fieldValue = application.found_unit;
                                            } else if (question.id === 'unitName') {
                                                fieldValue = application.unit_name;
                                            } else {
                                                fieldValue = application[question.id];
                                            }
                                        %>
                                        <% if (fieldValue) { %>
                                            <% if (question.type === 'textarea') { %>
                                                <div style="white-space: pre-wrap;"><%= fieldValue %></div>
                                            <% } else { %>
                                                <%= fieldValue %>
                                            <% } %>
                                        <% } else { %>
                                            <div class="no-response">No response provided</div>
                                        <% } %>
                                    </div>
                                </div>
                            <% }) %>

                            <!-- Admin Notes -->
                            <% if (application.notes) { %>
                                <h5 class="section-header mt-4">
                                    <i class="bi bi-sticky"></i>Admin Notes
                                </h5>
                                <div class="notes-section">
                                    <div class="notes-content"><%= application.notes %></div>
                                </div>
                            <% } %>

                            <!-- Action Buttons -->
                            <div class="d-flex justify-content-between mt-5 pt-4 border-top">
                                <div class="d-flex gap-3">
                                    <a href="/apply/admin/edit/<%= application.id %>" class="btn btn-warning btn-custom">
                                        <i class="bi bi-pencil-square me-1"></i>Edit Application
                                    </a>
                                    <button class="btn btn-danger btn-custom" onclick="deleteApplication('<%= application.id %>', '<%= application.username %>')">
                                        <i class="bi bi-trash me-1"></i>Delete Application
                                    </button>
                                </div>
                                <a href="/apply" class="btn btn-outline-secondary btn-custom">
                                    <i class="bi bi-arrow-left me-1"></i>Back to Applications
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <%- include("partials/footer") %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function deleteApplication(appId, username) {
            if (confirm(`Are you sure you want to delete the application for ${username}? This action cannot be undone.`)) {
                fetch(`/apply/admin/delete/${appId}`, {
                    method: 'DELETE',
                    headers: {
                        'Content-Type': 'application/json',
                    }
                })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        window.location.href = '/apply?alert=Application deleted successfully';
                    } else {
                        alert('Failed to delete application: ' + data.error);
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    alert('An error occurred while deleting the application');
                });
            }
        }
    </script>
</body>
</html> 