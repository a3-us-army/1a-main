<!DOCTYPE html>
<html>
  <head>
    <title>1A Website</title>
    <meta content="1A Website" property="og:title" />
    <meta content="Welcome to 1A!" property="og:description" />
    <meta content="https://1a75.org" property="og:url" />
    <meta content="https://cdn.xanderxx.xyz/1a-logo.png" property="og:image" />
    <meta content="#4b5a2a" data-react-helmet="true" name="theme-color" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css"
    />
    <link rel="icon" href="/favicon.ico" />
    <link rel="stylesheet" href="/custom.css" />
    <style>
      :root {
        --primary-gold: var(--ranger-gold, #e1b84c);
        --secondary-gold: var(--ranger-green, #4b5a2a);
        --dark-gold: var(--ranger-gold, #e1b84c);
        --deep-brown: var(--ranger-red, #b12a2c);
        --pure-black: var(--ranger-bg, #241d1d);
        --bg-gradient: linear-gradient(135deg, var(--ranger-bg, #241d1d) 0%, var(--ranger-footer, #291e1e) 100%);
        --card-bg: var(--ranger-card, #2d2d2d);
        --glass-bg: rgba(255, 255, 255, 0.1);
        --border-light: rgba(225, 184, 76, 0.3);
        --shadow-light: 0 8px 32px rgba(0, 0, 0, 0.1);
        --shadow-medium: 0 12px 40px rgba(0, 0, 0, 0.15);
        --shadow-heavy: 0 20px 60px rgba(0, 0, 0, 0.2);
      }

      body {
        background: var(--bg-gradient);
        min-height: 100vh;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        color: #ffffff;
      }

      .home-container {
        padding: 1rem 0;
      }

      .hero-section {
        background: var(--card-bg);
        border-radius: 24px;
        box-shadow: var(--shadow-heavy);
        overflow: hidden;
        border: 2px solid var(--ranger-gold);
        backdrop-filter: blur(20px);
        position: relative;
        margin-bottom: 3rem;
        padding: 3rem 2rem 2.5rem 2rem;
        margin-top: 2rem;
      }
      .hero-section::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 4px;
        background: linear-gradient(90deg, #FFD700, #DAA520, #B8860B);
      }
      .hero-title {
        font-size: 2.5rem;
        font-weight: 800;
        color: var(--primary-gold);
        margin-bottom: 1rem;
        text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
        line-height: 1.2;
        position: relative;
        z-index: 1;
      }
      .hero-desc {
        font-size: 1.2rem;
        font-weight: 400;
        margin-bottom: 0;
        color: #e0e0e0;
        opacity: 0.9;
        position: relative;
        z-index: 1;
        line-height: 1.6;
      }

      .hero-logo {
        height: 128px;
        width: auto;
        margin-bottom: 1.5rem;
        border-radius: 16px;
        border: 3px solid var(--ranger-gold);
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
      }

      .hero-logo:hover {
        transform: scale(1.05);
        box-shadow: 0 12px 35px rgba(225, 184, 76, 0.3);
      }
      .quick-link-card {
        background: rgba(255, 255, 255, 0.05);
        border-radius: 16px;
        border: 1px solid var(--ranger-gold);
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
        overflow: hidden;
        text-decoration: none;
        color: #ffffff;
        box-shadow: var(--shadow-medium);
      }

      .quick-link-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 3px;
        background: linear-gradient(90deg, #FFD700, #DAA520);
        transform: scaleX(0);
        transition: transform 0.3s ease;
      }

      .quick-link-card:hover {
        background: rgba(255, 255, 255, 0.1);
        transform: translateY(-4px) scale(1.02);
        box-shadow: var(--shadow-heavy);
        border-color: var(--ranger-gold);
        color: #ffffff;
        text-decoration: none;
      }

      .quick-link-card:hover::before {
        transform: scaleX(1);
      }
      .quick-link-card .card-body {
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.15rem;
        font-weight: 600;
        padding: 1.5rem 1rem;
        gap: 0.8rem;
        position: relative;
        z-index: 1;
      }

      .quick-link-card i {
        font-size: 1.4rem;
        color: var(--ranger-gold);
        transition: all 0.3s ease;
      }

      .quick-link-card:hover i {
        transform: scale(1.1);
        color: var(--ranger-gold);
      }

      /* Quick links grid improvements */
      .quick-links-grid {
        margin-top: 2rem;
      }

      /* Animations */
      .fade-in {
        animation: fadeIn 0.8s ease-out;
      }

      .slide-in {
        animation: slideIn 0.6s ease-out;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @keyframes slideIn {
        from {
          opacity: 0;
          transform: translateX(-40px);
        }
        to {
          opacity: 1;
          transform: translateX(0);
        }
      }

      /* Staggered card animations */
      .quick-link-card {
        animation: slideIn 0.6s ease-out;
        animation-fill-mode: both;
      }

      .quick-links-grid .col-12:nth-child(1) .quick-link-card { animation-delay: 0.1s; }
      .quick-links-grid .col-12:nth-child(2) .quick-link-card { animation-delay: 0.2s; }
      .quick-links-grid .col-12:nth-child(3) .quick-link-card { animation-delay: 0.3s; }
      .quick-links-grid .col-12:nth-child(4) .quick-link-card { animation-delay: 0.4s; }
      .quick-links-grid .col-12:nth-child(5) .quick-link-card { animation-delay: 0.5s; }
      @media (max-width: 768px) {
        .home-container {
          padding: 1rem 0;
        }

        .hero-section {
          padding: 2rem 1.5rem;
          margin-top: 1rem;
          margin-bottom: 2rem;
        }
        
        .hero-title {
          font-size: 2rem;
        }
        
        .hero-desc {
          font-size: 1.1rem;
        }

        .hero-logo {
          height: 100px;
          margin-bottom: 1rem;
        }
        
        .quick-link-card .card-body {
          font-size: 1rem;
          padding: 1.25rem 0.8rem;
        }

        .quick-link-card i {
          font-size: 1.2rem;
        }
      }

      @media (max-width: 480px) {
        .hero-section {
          padding: 1.5rem 1rem;
          margin-top: 0.5rem;
        }
        
        .hero-title {
          font-size: 1.75rem;
        }
        
        .hero-desc {
          font-size: 1rem;
        }

        .hero-logo {
          height: 80px;
        }
        
        .quick-link-card .card-body {
          font-size: 0.95rem;
          padding: 1rem 0.6rem;
        }

        .quick-link-card i {
          font-size: 1.1rem;
        }
      }
    </style>
  </head>
  <body>
    <%- include("../partials/navbar", { user, active }) %>
    <main id="main-content">
      <div class="container home-container">
        <div class="hero-section text-center mb-5 fade-in">
          <img
            src="https://cdn.xanderxx.xyz/1a-logo.png"
            alt="1A Logo"
            class="hero-logo"
          />
          <h1 class="hero-title mb-3">
            <i class="bi bi-shield-check me-3"></i>
            Welcome to 1A Administration
          </h1>
          <p class="hero-desc mb-0">
            Your central hub for 1A administration and personnel management.<br />
            Access your dashboard, view personnel roles, and manage your profile.
          </p>
        </div>
        <div class="row g-4 justify-content-center quick-links-grid">
          <div class="col-12 col-sm-6 col-md-4 col-lg-3">
            <a
              href="/staff"
              class="card quick-link-card text-decoration-none"
              aria-label="Meet the Staff"
            >
              <div class="card-body">
                <i class="bi bi-people"></i> Meet the Staff
              </div>
            </a>
          </div>
          <div class="col-12 col-sm-6 col-md-4 col-lg-3">
            <a
              href="/about"
              class="card quick-link-card text-decoration-none"
              aria-label="About Us"
            >
              <div class="card-body">
                <i class="bi bi-info-circle"></i> About
              </div>
            </a>
          </div>

          <div class="col-12 col-sm-6 col-md-4 col-lg-3">
            <a
              href="/personnel"
              class="card quick-link-card text-decoration-none"
              aria-label="View All Personnel"
            >
              <div class="card-body"><i class="bi bi-people"></i> PERSTAT</div>
            </a>
          </div>
          <div class="col-12 col-sm-6 col-md-4 col-lg-3">
            <a
              href="/dashboard"
              class="card quick-link-card text-decoration-none"
              aria-label="Dashboard"
            >
              <div class="card-body">
                <i class="bi bi-speedometer2"></i> Dashboard
              </div>
            </a>
          </div>
          <% if (user) { %>
            <div class="col-12 col-sm-6 col-md-4 col-lg-3">
            <a
              href="/apply"
              class="card quick-link-card text-decoration-none"
              aria-label="Application"
            >
              <div class="card-body">
                <i class="bi bi-file-earmark-text"></i> Application
              </div>
            </a>
          </div>
          <div class="col-12 col-sm-6 col-md-4 col-lg-3">
            <a
              href="/profile"
              class="card quick-link-card text-decoration-none"
              aria-label="Your Profile"
            >
              <div class="card-body">
                <i class="bi bi-person-circle"></i> PERSEC
              </div>
            </a>
          </div>
          <% } else { %>
          <div class="col-12 col-sm-6 col-md-4 col-lg-3">
            <a
              href="/login"
              class="card quick-link-card text-decoration-none"
              aria-label="Login with Discord"
            >
              <div class="card-body">
                <i class="bi bi-discord"></i> Login with Discord
              </div>
            </a>
          </div>
          <% } %>
        </div>
      </div>
    </main>
    <%- include("../partials/footer") %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
