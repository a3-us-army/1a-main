<!DOCTYPE html>
<html>
  <head>
    <title>Dashboard</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta content="1A Website" property="og:title" />
    <meta content="Welcome to 1A!" property="og:description" />
    <meta content="https://1a75.org/dashboard" property="og:url" />
    <meta content="https://cdn.xanderxx.xyz/1a-logo.png" property="og:image" />
    <meta content="#4b5a2a" data-react-helmet="true" name="theme-color" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="icon" href="/favicon.ico" />
    <link rel="stylesheet" href="/custom.css" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css"
    />
    <style>
      .dashboard-btn {
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.25rem;
        font-weight: 500;
        padding: 1.5rem 1.2rem;
        border-radius: 0.7rem;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
        transition:
          background 0.15s,
          color 0.15s,
          transform 0.12s;
        background: #23272f;
        color: #ffe066;
        border: none;
        margin-bottom: 1.2rem;
        text-decoration: none;
      }
      .dashboard-btn i {
        font-size: 1.7rem;
        margin-right: 0.9rem;
      }
      .dashboard-btn:hover,
      .dashboard-btn:focus {
        background: #b68900;
        color: #fffbe6;
        transform: translateY(-2px) scale(1.03);
        text-decoration: none;
      }
      @media (max-width: 600px) {
        .dashboard-btn {
          width: 100% !important;
          font-size: 1.1rem;
          padding: 1.1rem 0.7rem;
        }
      }
    </style>
  </head>
  <body>
    <%- include("../partials/navbar", { user, active }) %>
    <main id="main-content">
      <div class="container mt-5">
        <h2 class="mb-4 text-center">Dashboard</h2>
        <div class="row justify-content-center">
          <div
            class="col-12 col-sm-8 col-md-6 col-lg-4 d-flex flex-column align-items-center"
          >
            <a
              href="/events"
              class="dashboard-btn w-100 mb-3"
              aria-label="Go to Events"
            >
              <i class="bi bi-calendar-event"></i> Events
            </a>
            <a
              href="/certs"
              class="dashboard-btn w-100"
              aria-label="Go to Certifications"
            >
              <i class="bi bi-patch-check"></i> Certifications
            </a>
            <a
              href="/modpacks"
              class="dashboard-btn w-100"
              aria-label="Go to Modpacks"
            >
              <i class="bi bi-download"></i> Modpacks
            </a>
            <a
              href="/personnel"
              class="dashboard-btn w-100"
              aria-label="Go to Personnel"
            >
              <i class="bi bi-person"></i>PERSTAT
            </a>
            <a
              href="/documents"
              class="dashboard-btn w-100"
              aria-label="Go to Document Center"
            >
              <i class="bi bi-file-earmark-text"></i>Document Center
            </a>
          </div>
        </div>
      </div>
    </main>
    <%- include("../partials/footer") %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
